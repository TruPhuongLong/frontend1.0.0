import {initState} from '../store/state'
import {

} from '../actions/type.action'

export const CampaignRegistrationReducer = (state = initState.campaignRegistrationState, action) => {
    switch(action.type){
        default:
            return state
    }
} 